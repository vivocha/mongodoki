{
  "name": "mongodoki",
  "version": "0.0.0-development",
  "description": "A tool to on-the-fly run MongoDB as a Docker Container, data restoring included",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "bin": {
    "mongodoki": "dist/bin.js"
  },
  "directories": {
    "test": "./test"
  },
  "scripts": {
    "clean": "rimraf dist coverage .nyc_output",
    "clean:all": "npm run clean && rimraf node_modules",
    "prebuild": "rimraf dist",
    "build": "tsc",
    "precommit": "npm run build && npm run cover && npm run check-coverage && rimraf test/testdb",
    "commit": "git-cz",
    "check-coverage": "./node_modules/.bin/nyc check-coverage --statements 95 --branches 85 --functions 100 --lines 95",
    "report-coverage": "cat ./coverage/lcov.info | coveralls",
    "watch:test": "npm t -- -w",
    "test": "mocha --no-timeouts test/*.test.js",
    "cover": "./node_modules/.bin/nyc --reporter=lcov --reporter=text npm t",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vivocha/mongodoki.git"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "author": "Antonio Pintus <apintus@vivocha.com> (http://www.vivocha.com)",
  "license": "MIT",
  "keywords": [
    "Docker",
    "MongoDB",
    "Containers"
  ],
  "devDependencies": {
    "@types/chai": "^4.0.2",
    "@types/chai-as-promised": "0.0.31",
    "@types/chai-spies": "0.0.0",
    "@types/dockerode": "^2.4.5",
    "@types/mocha": "^2.2.41",
    "@types/node": "^8.0.20",
    "chai": "^4.1.1",
    "chai-as-promised": "^7.1.1",
    "chai-spies": "^0.7.1",
    "commitizen": "^2.9.6",
    "coveralls": "^2.13.1",
    "cz-conventional-changelog": "^2.0.0",
    "husky": "^0.14.3",
    "mocha": "^3.5.0",
    "nyc": "^11.1.0",
    "rimraf": "^2.6.1",
    "semantic-release": "^6.3.6",
    "typescript": "^2.4.2"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "@types/commander": "^2.9.1",
    "@types/debug": "0.0.30",
    "@types/mongodb": "^2.2.9",
    "@types/ora": "^1.3.0",
    "commander": "^2.11.0",
    "debug": "^3.0.0",
    "dockerode": "^2.4.3",
    "mongodb": "^2.2.31",
    "ora": "^1.3.0"
  }
}
